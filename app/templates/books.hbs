<ul>
  {{#each model as |book|}}
    {{!-- {{book.title}} --}}
    {{!-- {{book-cover book=book}} --}}

    <li>
      <strong>{{book.title}}</strong><br><br>
      <em>by</em>
      {{book.author.name}}
    </li>
  {{/each}}
</ul>

{{!-- {{outlet}} --}}